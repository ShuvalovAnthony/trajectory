import { createSignal, createResource, createEffect, For } from "solid-js";

const Planpage = () => {
    return (
        <div>
            fsdfds
        </div>

    );
};

export default Planpage; 
